# -*- mode: snippet -*-
# name: chunk
# key: chu
# --
chunk${1:$(if (> (length text) 0) "(" "")}${1:initial}${1:$(if (> (length text) 0) ")" "")} { |${2:e}${3:$(if (> (length text) 0) ", " "")}${3:state}| $0 }