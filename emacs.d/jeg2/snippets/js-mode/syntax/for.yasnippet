# -*- mode: snippet -*-
# name: for
# key: for
# --
for (${1:var ${2:i} = ${3:0}};${4:$(if (> (length text) 0) " " "")}${4:$2 < ${5:${6:a}.length}};${7:$(if (> (length text) 0) " " "")}${7:$2++}) {
   $0
}