# -*- mode: snippet -*-
# name: expect not_to raise_error
# key: expntre
# --
expect do
  $0
end.not_to raise_error(${1:Exception}${2:$(if (> (length text) 0) ", \\\"" "")}${2:Failure message}${2:$(if (> (length text) 0) "\\\"" "")})