(defun jeg2/set-full-size ()
  "Size frame for normal work."
  (interactive)
  (set-frame-size (selected-frame) 164 69))

(defun jeg2/set-streaming-size ()
  "Size frame for streaming."
  (interactive)
  (set-frame-size (selected-frame) 126 35))
