# -*- mode: snippet -*-
# name: window.setTimeout
# key: sett
# --
${1:$(if (> (length yas-text) 0) "var " "")}${1:timeoutId}${1:$(if (> (length yas-text) 0) " = " "")}window.setTimeout( function() {
    $0
}, ${2:5000});