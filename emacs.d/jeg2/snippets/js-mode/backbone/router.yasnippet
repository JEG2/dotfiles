# -*- mode: snippet -*-
# name: Backbone.Router
# key: rou
# --
${1:$(if (> (length yas-text) 0) "var " "")}${1:window.${2:Name}}${1:$(if (> (length yas-text) 0) " = " "")}Backbone.Router.extend( {
    routes: {
        "${3:path/:var}": "${4:method}"$0
    }
} );