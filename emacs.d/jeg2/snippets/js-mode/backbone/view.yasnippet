# -*- mode: snippet -*-
# name: Backbone.View
# key: vie
# --
${1:$(if (> (length yas-text) 0) "var " "")}${1:window.${2:Name}}${1:$(if (> (length yas-text) 0) " = " "")}Backbone.View.extend( {
    $0
} );