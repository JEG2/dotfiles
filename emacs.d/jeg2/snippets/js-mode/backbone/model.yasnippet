# -*- mode: snippet -*-
# name: Backbone.Model
# key: mod
# --
${1:$(if (> (length yas-text) 0) "var " "")}${1:window.${2:Name}}${1:$(if (> (length yas-text) 0) " = " "")}Backbone.Model.extend( {
    $0
} );